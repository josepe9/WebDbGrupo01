@model IEnumerable<WebDbGrupo01.Models.MyDb.Programa>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Niveles)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Niveles)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProgramaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProgramaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProgramaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
